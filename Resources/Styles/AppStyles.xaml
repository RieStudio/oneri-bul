<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- Specific App Styles as requested -->
    <Color x:Key="AppBackground">#101014</Color>
    <Color x:Key="AppSurface">#1A1A1E</Color>
    <Color x:Key="AppPrimaryAccent">#4A6CFF</Color>
    <Color x:Key="AppSecondaryAccent">#8A2BE2</Color>

    <Style x:Key="DiscoveryButton" TargetType="Button">
        <Setter Property="Background">
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                <GradientStop Color="#4A6CFF" Offset="0.1" />
                <GradientStop Color="#8A2BE2" Offset="1.0" />
            </LinearGradientBrush>
        </Setter>
        <Setter Property="TextColor" Value="White" />
        <Setter Property="CornerRadius" Value="14" />
        <Setter Property="HeightRequest" Value="60" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup Name="CommonStates">
                    <VisualState Name="Normal" />
                    <VisualState Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Scale" Value="0.95" />
                            <Setter Property="Opacity" Value="0.8" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="RecommendationCard" TargetType="Border">
        <Setter Property="BackgroundColor" Value="{StaticResource AppSurface}" />
        <Setter Property="StrokeShape" Value="RoundRectangle 20" />
        <Setter Property="StrokeThickness" Value="0" />
        <Setter Property="Padding" Value="15" />
        <Setter Property="Margin" Value="0,20" />
        <Setter Property="Shadow">
            <Shadow Brush="Black" Offset="4,4" Radius="10" Opacity="0.4" />
        </Setter>
    </Style>

</ResourceDictionary>
